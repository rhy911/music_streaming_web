When pull to new device, must reinstall node_modules: cd to server and npm init -y
To run server: nodemon start

SQL command to delete whole table data:
    SET FOREIGN_KEY_CHECKS = 0;
    TRUNCATE TABLE users;
    SET FOREIGN_KEY_CHECKS = 1;
